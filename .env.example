# Copy this file to ".env" and fill in your real values.
#   cp .env.example .env      (Git Bash)
#   copy .env.example .env    (cmd / PowerShell)
#
# .env is gitignored - it must NEVER be committed.
# generate_qa.py loads it automatically via python-dotenv.

AZURE_OPENAI_KEY=replace-with-your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://muham-mp9jnggo-eastus2.cognitiveservices.azure.com/
